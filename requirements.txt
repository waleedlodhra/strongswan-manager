# Core Django stack
Django>=4.2,<6.0
djangorestframework>=3.14
django-tables2>=2.3

# Encryption / PKI
git+https://github.com/wbond/oscrypto.git@1547f535001ba568b239b8797465536759c742a3
asn1crypto>=1.5.1
cryptography>=40.0
pyaes>=1.6.1

# VICI (StrongSwan management API)
vici>=5.8

# ASGI / WebSocket (Daphne is included via channels[daphne])
channels[daphne]>=4.0

# Static file serving over ASGI (wsgi.py fallback)
dj-static>=0.0.6

# File watching for config sync
watchdog>=3.0

# Background task scheduler (no broker required)
APScheduler>=3.10
